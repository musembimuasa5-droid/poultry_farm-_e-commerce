<?php require_once 'config.php'; if(current_user()) redirect('account.php'); $error=''; if($_SERVER['REQUEST_METHOD']==='POST'&&verify_csrf($_POST['csrf']??null)){ $stmt=db()->prepare('SELECT * FROM users WHERE email=? AND status="active" LIMIT 1');$stmt->execute([trim($_POST['email'])]);$user=$stmt->fetch(); if($user&&password_verify($_POST['password'],$user['password_hash'])){session_regenerate_id(true);$_SESSION['user']=$user;redirect('account.php');}$error='Those details did not match an active account.';} $pageTitle='Sign in';include 'includes/header.php'; ?>
<section class="section"><div class="container" style="max-width:540px"><div class="form-card"><div class="eyebrow">Welcome back</div><h1>Sign in.</h1><p>Pick up where you left off with the farm.</p><?php if($error): ?><div class="toast-error notice"><?= e($error) ?></div><?php endif; ?><form method="post"><input type="hidden" name="csrf" value="<?= e(csrf_token()) ?>"><div class="form-group"><label>Email</label><input class="form-control" type="email" name="email" required></div><div class="form-group"><label>Password</label><input class="form-control" type="password" name="password" required minlength="8"></div><button class="btn btn-primary">Sign in →</button></form><p style="margin-top:20px">New here? <a class="text-link" href="register.php">Create an account</a></p></div></div></section><?php include 'includes/footer.php'; ?>
